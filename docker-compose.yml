services:
  mcp-api:
    build: .
    container_name: mcp-api
    env_file:
      - stack.env
    ports:
      - "6666:3006"
    restart: unless-stopped
    networks:
      - mcp-network
      - financer-transactions_transactions-network

networks:
  mcp-network:
    driver: bridge
  financer-transactions_transactions-network:
    external: true
