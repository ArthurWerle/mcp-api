services:
  mcp-api:
    build: .
    container_name: mcp-api
    env_file:
      - stack.env
    ports:
      - "6666:3001"
    restart: unless-stopped
    networks:
      - mcp-network
      - transactions_transactions-network

networks:
  mcp-network:
    driver: bridge
  transactions_transactions-network:
    external: true
